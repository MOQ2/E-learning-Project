/* نجاح */
.p-toast-message-success {
  background: #d1f7d6;   /* أخضر فاتح */
  border-left: 5px solid #28a745; /* أخضر غامق */
  color: #155724;
  font-weight: 500;
}

/* خطأ */
.p-toast-message-error {
  background: #f8d7da;   /* أحمر فاتح */
  border-left: 5px solid #dc3545; /* أحمر غامق */
  color: #721c24;
  font-weight: 500;
}

/* تحذير */
.p-toast-message-warn {
  background: #fff3cd;
  border-left: 5px solid #ffc107;
  color: #856404;
  font-weight: 500;
}

/* معلومة */
.p-toast-message-info {
  background: #cce5ff;
  border-left: 5px solid #007bff;
  color: #004085;
  font-weight: 500;
}
